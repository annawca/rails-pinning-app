<div class="pins">
  <% @pins.each_slice(3) do |row| %>
    <div class="row">
      <% row.each do |pin| %>      
        <div class="pin-container col-md-4 col-sm-6">
          <div class="pin">
            <div class="pin-header row">

              <div class="logo-thumbnail col-xs-4">
               <% if "#{pin.category.name}" == "rails" %>
                  <%= image_tag('rails-logo-thumbnail.png') %>
                <% elsif "#{pin.category.name}" == "ruby" %>    
                  <%= image_tag('ruby-logo-thumbnail.png') %> 
                <% end %>
                <% if !pin.image.url.nil? %>
                  <img src="<%= pin.image.url(:thumb) %>">
                <% end %>
              </div>

              <div class="pin-title col-xs-8">
               <h3>
                <%= link_to "#{pin.title}", pin_by_name_path(slug: pin.slug) %>
              </h3>
               <%= link_to "#{pin.url.truncate(30)}", "#{pin.url}", html_options = {target: "_blank"} %>
               </div>
              </div>
            <hr/>
            <div>
            <p class="text"><%= "#{pin.text}" %></p>
            <%= render partial: "shared/repin_form", locals: {pin: pin} %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
